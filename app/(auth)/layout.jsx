// app/(auth)/layout.jsx
export default function AuthLayout({ children }) {
    return (
      // You can add specific layout elements for auth pages here if needed
      // For now, it just renders the children (e.g., the LoginPage)
      <>{children}</>
    );
  }
  